<div class="container-fluid bg-dark text-white py-5 mb-5 shadow" style="background: linear-gradient(45deg, #1a1a1a 0%, #343a40 100%);">
    <div class="container py-5 text-center">
        <h1 class="display-3 fw-bold mb-4">Най-мощните <span class="text-primary">GPU</span> на едно място</h1>
        <p class="lead mb-5 fs-4 text-light-50">Открий следващата си видеокарта за гейминг или професионална работа.</p>

        <div class="d-grid gap-3 d-sm-flex justify-content-sm-center">
            @if (User.Identity.IsAuthenticated)
            {
                @* Ако е логнат (Клиент) - директно в каталога *@
                <a asp-controller="VideoCards" asp-action="UserIndex" class="btn btn-primary btn-lg px-5 py-3 fw-bold rounded-pill shadow">
                    <i class="bi bi-shop me-2"></i> КЪМ КАТАЛОГА
                </a>
            }
            else
            {
                @* Ако не е логнат (Гост) - към Логин формата *@
                <a asp-area="Identity" asp-page="/Account/Login" class="btn btn-warning btn-lg px-5 py-3 fw-bold rounded-pill shadow">
                    <i class="bi bi-box-arrow-in-right me-2"></i> ВЛЕЗ И ПАЗАРУВАЙ
                </a>
            }
        </div>
    </div>
</div>

<div class="container my-5 py-5">
    <div class="row g-4 text-center">
        <div class="col-md-4">
            <i class="bi bi-truck fs-1 text-primary"></i>
            <h3 class="mt-3">Бърза доставка</h3>
            <p class="text-muted">Доставяме до всяка точка на страната в рамките на 24 часа.</p>
        </div>
        <div class="col-md-4">
            <i class="bi bi-shield-check fs-1 text-primary"></i>
            <h3 class="mt-3">Гаранция</h3>
            <p class="text-muted">Всички продукти идват с пълна производствена гаранция.</p>
        </div>
        <div class="col-md-4">
            <i class="bi bi-headset fs-1 text-primary"></i>
            <h3 class="mt-3">Поддръжка</h3>
            <p class="text-muted">Нашите експерти са на линия, за да ви помогнат с избора.</p>
        </div>
    </div>
</div>